apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloud-native-chart.fullname" . }}-test-connection"
  labels:
    {{- include "cloud-native-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloud-native-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
